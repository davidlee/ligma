---
id: DE-007
slug: adopt_strict_lint_config_from_docs_lint_md
name: Delta - Adopt strict lint config from docs/lint.md
created: '2026-03-10'
updated: '2026-03-10'
status: draft
kind: delta
aliases: []
relations:
- type: depends-on
  target: DE-002
context_inputs:
  - docs/lint.md
applies_to:
  specs:
  - SPEC-001
  requirements:
  - PROD-001.NF-004
---

# DE-007 – Adopt strict lint config from docs/lint.md

```yaml supekku:delta.relationships@v1
schema: supekku.delta.relationships
version: 1
delta: DE-007
revision_links:
  introduces: []
  supersedes: []
specs:
  primary:
    - SPEC-001
  collaborators: []
requirements:
  implements:
    - PROD-001.NF-004
  updates: []
  verifies: []
phases: []
```

## 1. Summary & Context
- **Technical Spec**: SPEC-001 – Figma fetch and normalization system
- **Implementation Plan**: [IP-007](./IP-007.md)
- **Change Drivers**: ADR-001 (zero tolerance for lint warnings), `docs/lint.md` (canonical lint config)

## 2. Motivation
Replace the minimal eslint config from DE-002 scaffold with the strict, opinionated config defined in `docs/lint.md`. Enforces ADR-001: zero warnings, binary pass/fail. Adds type-checked rules, import ordering, dead code detection, complexity limits, and naming conventions.

## 3. Scope & Objectives
- **Primary Outcomes**:
  - `eslint.config.js` implements `docs/lint.md` fully (strictTypeChecked, stylistic, import, unicorn, unused-imports)
  - All existing code (`src/errors.ts`, `src/figma/url.ts`, tests) passes the strict config
  - Additional tsconfig flags from `docs/lint.md` applied (`noImplicitOverride`, `useUnknownInCatchVariables`)
  - New devDependencies installed: `eslint-plugin-import`, `eslint-plugin-unicorn`, `eslint-plugin-unused-imports`
- **Dependencies**: DE-002 PHASE-01 complete (scaffold exists)

## 4. Out of Scope
- CI pipeline integration (future)
- Pre-commit hooks (future)

## 5. Approach Overview
- **System Touchpoints**: `eslint.config.js`, `package.json`, `tsconfig.json`, `src/**`, `tests/**`
- **Key Changes**:
  - Replace eslint.config.js with full config from docs/lint.md
  - Install additional eslint plugins
  - Fix any lint violations in existing code
  - Add tsconfig strict flags

## 6. Verification Strategy
- `pnpm lint` exits 0
- `pnpm test` still passes (no regressions)
- `pnpm typecheck` still passes
- No `warn` severity in eslint config

## 7. Risks & Mitigations
- **Risk**: Strict rules conflict with existing code patterns – *Likelihood*: medium – *Impact*: low – *Mitigation*: Fix violations; relax only per docs/lint.md guidance (explicit-function-return-type, max-lines-per-function)
