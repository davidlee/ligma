---
id: IP-008
slug: backlog_fixes_individualstrokeweights_and_interaction_extraction
name: 'Implementation Plan - Backlog fixes: individualStrokeWeights and interaction
  extraction'
created: '2026-03-11'
updated: '2026-03-11'
status: active
kind: plan
aliases: []
---

```yaml supekku:plan.overview@v1
schema: supekku.plan.overview
version: 1
plan: IP-008
delta: DE-008
revision_links:
  aligns_with:
  - DR-008
specs:
  primary:
  - SPEC-001
  collaborators: []
requirements:
  targets:
  - SPEC-001.FR-006
  dependencies: []
phases:
- id: IP-008.PHASE-01
```

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: IP-008
entries:
  - artefact: VT-010
    kind: VT
    requirement: SPEC-001.FR-006
    status: verified
    notes: Stroke weight per-side support — 7 updated tests (DE-008)
  - artefact: VT-024
    kind: VT
    requirement: SPEC-001.FR-018
    status: verified
    notes: Interaction extraction — 30 tests (DE-008)
```

## 1. Summary
- **Delta**: DE-008 — Per-side stroke weights and interaction extraction
- **Specs Impacted**: SPEC-001 (normalization engine)
- **Problems / Issues**: ISSUE-001 (individualStrokeWeights dropped), ISSUE-002 (interactions not extracted)
- **Desired Outcome**: Normalized output includes per-side stroke weights and interaction data from Figma nodes.

## 2. Context & Constraints
- **Current Behaviour**: Scalar `strokeWeight` only; `interactions` array ignored.
- **Target Behaviour**: FR-006 appearance normalization extended with per-side strokes; new interaction extraction concern.
- **Dependencies**: None — builds on completed DE-003/DE-004 normalization work.
- **Constraints**: Additive for interactions; contained breaking change for stroke weight (no downstream consumers).

## 3. Gate Check
- [x] Backlog items linked (ISSUE-001, ISSUE-002)
- [x] DR-008 approved with design decisions DEC-030 through DEC-033
- [x] Test strategy identified (VT-010 extension + new VT-020)
- [x] No workspace/config changes needed

## 4. Phase Overview
| Phase | Objective | Entrance Criteria | Exit Criteria | Phase Sheet |
| --- | --- | --- | --- | --- |
| Phase 1 — Implementation | Schema + extractors + tests | DR-008 approved, IP active | `mise run` passes, all VT green | `phases/phase-01.md` |

Single phase — both work streams are small, independent at file level, and share the same quality gate.

## 5. Testing & Verification Plan
- **Updated Suites**: `tests/normalize/appearance.test.ts` (VT-010 stroke weight cases)
- **New Suites**: `tests/normalize/interactions.test.ts` (VT-020)
- **Key Scenarios**: Per-side weight, uniform collapse, zero semantics, precedence, trigger/action mapping, unknown types, malformed entries
- **Tooling/Fixtures**: Existing `makeNode()` helper; verify action mapping against Figma REST fixtures

## 6. Risks & Mitigations
| Risk | Mitigation |
| --- | --- |
| Raw action field names differ from docs | Verify against captured REST fixtures during implementation |
| Malformed `individualStrokeWeights` | Warn and fall back to scalar |
| Missing `trigger.type` on interaction entry | Skip with warning |

## 7. Open Questions & Decisions
None remaining — all resolved in DR-008.

## 8. Progress Tracking
- [ ] Phase 1 complete
- [ ] Verification gates passed
- [ ] Contract sync
