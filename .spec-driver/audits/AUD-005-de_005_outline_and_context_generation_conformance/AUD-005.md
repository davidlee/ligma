---
id: AUD-005
slug: de_005_outline_and_context_generation_conformance
name: Audit - DE-005 outline and context generation conformance
created: '2026-03-11'
updated: '2026-03-11'
status: completed
kind: audit
mode: conformance
delta_ref: DE-005
spec_refs:
- SPEC-001
prod_refs:
- PROD-001
code_scope:
- src/schemas/outline.ts
- src/normalize/outline.ts
- src/output/context-md.ts
- src/schemas/manifest.ts
- src/output/write.ts
- src/orchestrate.ts
- src/config.ts
- src/cli.ts
---

```yaml supekku:verification.coverage@v1
schema: supekku.verification.coverage
version: 1
subject: AUD-005
entries:
  - artefact: VT-018
    kind: VT
    requirement: PROD-001.FR-011
    status: verified
    notes: 15 tests — outline projection, hidden filtering, includeHidden, childCount, role/bounds
  - artefact: VT-019
    kind: VT
    requirement: PROD-001.FR-011
    status: verified
    notes: 17 tests — XML element naming (SPEC-001.FR-013 mapping), escaping, stable attributes
  - artefact: VT-020
    kind: VT
    requirement: PROD-001.FR-012
    status: verified
    notes: 36 tests — section generators, tiering, golden snapshot, conservative language
  - artefact: VT-021
    kind: VT
    requirement: PROD-001.NF-002
    status: verified
    notes: 5 tests — determinism for buildOutline, outlineToXml, generateContextMd, full pipeline
  - artefact: VT-006
    kind: VT
    requirement: PROD-001.FR-013
    status: verified
    notes: 22 tests — manifest schema with 6 required output fields (regression green)
  - artefact: VT-012
    kind: VT
    requirement: PROD-001.FR-005
    status: verified
    notes: 18 tests — orchestrate produces outline + context.md fields (regression green)
```

## Observations

### Requirement coverage

| Requirement | Implementation | Tests | Status |
| --- | --- | --- | --- |
| PROD-001.FR-011 (outline) | `src/normalize/outline.ts` — `buildOutline()` + `outlineToXml()` | VT-018 (15), VT-019 (17) | aligned |
| PROD-001.FR-012 (context.md) | `src/output/context-md.ts` — `generateContextMd()` | VT-020 (36) | aligned |
| PROD-001.NF-002 (determinism) | Pure functions, no side effects | VT-021 (5) | aligned |
| SPEC-001.FR-013 (XML vocab) | XML element name mapping table | VT-019 updated | aligned (after fix) |
| DEC-024 (manifest required) | `src/schemas/manifest.ts` — 6 required fields | VT-006 (22) | aligned |
| DEC-025 (--include-hidden) | `src/config.ts` + `src/cli.ts` end-to-end | orchestrate tests | aligned |

### Design decision compliance

| Decision | Status | Notes |
| --- | --- | --- |
| DEC-021 (hidden filtering) | aligned | Default omit, `includeHidden` restores with `visible="false"` |
| DEC-022 (important children) | aligned | 4-tier sort, document order tie-break, cap at 8 |
| DEC-023 (implementation notes) | aligned | 6 generators, cap at 5, directive language |
| DEC-024 (manifest required) | aligned | 6 required, 2 optional (png/svg), 1 array (assets) |
| DEC-025 (--include-hidden) | aligned | config → orchestrate → buildOutline → CLI flag |

### Pipeline integration

- `orchestrate()` produces `outlineJson`, `outlineXml`, `contextMd` — verified
- `writeOutput()` writes all 4 new files unconditionally — verified
- `OrchestrateResult` and `OutputArtifacts` interfaces match DR-005 §5.4/§5.6

## Evidence

- **Test suite**: 689 tests, 36 files, 0 failures (`mise run test`)
- **Lint**: 0 warnings (`mise run lint`)
- **Typecheck**: clean (`mise run typecheck`)
- **Regression**: VT-006 (22 tests), VT-012 (18 tests) confirmed green
- **Determinism**: VT-021 (5 tests) — `===` comparison for all output artifacts

## Findings

### F-001: XML element name mismatch (SPEC-001.FR-013)

- **Severity**: medium
- **Observed**: `boolean-operation` mapped to `<boolean-operation>`, `unknown` mapped to `<unknown>`
- **Expected**: `boolean-operation` → `<boolean-op>`, `unknown` → `<node>` per SPEC-001.FR-013 table
- **Outcome**: Fixed in this audit phase. `src/normalize/outline.ts` updated, test assertions updated in `tests/normalize/outline-xml.test.ts`
- **Disposition**: `aligned` (corrected during audit)

### F-002: No further findings

All other implementations match SPEC-001, PROD-001, DR-005, and DEC-021–025. No invented filenames in context.md. No "must/always" language from heuristics. Pure functions throughout. Deterministic output confirmed.

## Recommendations

None. All findings resolved. Delta is ready for closure.
